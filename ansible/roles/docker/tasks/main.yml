---
- name: Install Docker engine from Debian
  ansible.builtin.apt:
    name: docker.io
    state: present

- name: Enable Docker engine
  ansible.builtin.systemd_service:
    name: docker
    enabled: true
    state: started
